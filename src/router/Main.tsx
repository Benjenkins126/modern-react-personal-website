import { Routes, Route } from 'react-router-dom';

// import Page from '../pages/Page';

const Main = () => {
    return (
        <Routes>
             {/* <Route path='/page' element={<Page />} />*/}
        </Routes>
    );
}

export default Main;